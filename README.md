# utf8checker

```
$ ./utf8checker.py -h
usage: check_utf8.py [-h] [-r] path file_name

List non UTF-8 files and files with non LF line endings.

positional arguments:
  path        file with changes or pages to activate
  file_name   file name (shell-style pattern)

optional arguments:
  -h, --help  show this help message and exit
  -r          traverse the path recursively
cursively.

```
